const CANCEL = "cancel";
const CONFIRM = "confirm";
const INIT = "init";
const SEARCH = "search";
const TRACK = "track";

const BPP_API_URLS = {
    CANCEL,
    CONFIRM,
    INIT,
    SEARCH,
    TRACK,
}

export default BPP_API_URLS;