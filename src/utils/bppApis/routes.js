const CANCEL = "cancel";
const CONFIRM = "confirm";
const INIT = "init";
const SEARCH = "search";
const TRACK = "track";
const SUPPORT = "support";

const BPP_API_URLS = {
    CANCEL,
    CONFIRM,
    INIT,
    SEARCH,
    TRACK,
    SUPPORT,
}

export default BPP_API_URLS;