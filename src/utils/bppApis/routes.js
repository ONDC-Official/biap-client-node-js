const CANCEL = "cancel";
const CONFIRM = "confirm";
const INIT = "init";
const TRACK = "track";
const SUPPORT = "support";

const BPP_API_URLS = {
    CANCEL,
    CONFIRM,
    INIT,
    TRACK,
    SUPPORT,
}

export default BPP_API_URLS;