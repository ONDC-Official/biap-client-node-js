const CANCEL = "cancel";
const CONFIRM = "confirm";
const INIT = "init";
const TRACK = "track";
const SUPPORT = "support";
const STATUS = "status";

const BPP_API_URLS = {
    CANCEL,
    CONFIRM,
    INIT,
    TRACK,
    SUPPORT,
    STATUS
}

export default BPP_API_URLS;